name: Python application

on:
  push:
   branches: [ master ]
  pull_request:
    branches: [ master ]
      
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build & Push Image
      run: |
        echo "${{ secrets.DOCKERPW }}" | docker login -u "deepthii28" --password-stdin
        docker image build -t deepthii28/pyflask1:v1 .
        docker push deepthii28/pyflask1:v1
        
